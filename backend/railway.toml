[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn phg_build_ia_backend:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production]
