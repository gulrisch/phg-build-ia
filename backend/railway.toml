[build]
builder = "nixpacks"

[deploy]
startCommand = "alembic upgrade head && uvicorn phg_build_ia_backend:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
